$(document).ready(function () {

    $('a.delete').click(function (event) {

    });

});

//var buffer_req = new Buffer([0x16, 0x31, 0x30, 0x30, 0x30, 0x30, 0x32, 0x30, 0x30, 0xFC]);
// var stream = new BufferStream(buffer_req);
// var client = net.connect({ host: 'someIp.pl', port: 7171 }, connected);
